<Window x:Class="Tests_SGMU.Desktop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:Tests_SGMU.Desktop.ViewModels"
        xmlns:views="clr-namespace:Tests_SGMU.Desktop.Views"
        Title="Медицинские тестики" 
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Window.DataContext>
        <viewModels:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>

        <DataTemplate DataType="{x:Type viewModels:MainMenuViewModel}">
            <views:MainMenuView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:TestSelectionViewModel}">
            <views:TestSelectionView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type  viewModels:AllQuestionsViewModel}">
            <views:AllQuestionsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:ExamViewModel}">
            <views:ExamView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <!-- Панель навигации с кнопкой "Назад" -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Кнопка "Назад" - видна только когда можно вернуться -->
        <Button Content="⬅ Назад" 
                Command="{Binding GoBackCommand}"
                Style="{StaticResource ModernButton}"
                Width="80"
                HorizontalAlignment="Left"
                Margin="10"
                Grid.Row="0"
                Visibility="{Binding CanGoBack, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <!-- Основной контент -->
        <ContentControl Content="{Binding CurrentView}" Grid.Row="1"/>
    </Grid>
</Window>